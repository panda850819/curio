# HTTP server
HOST=0.0.0.0
PORT=3000

# Docker stores this file on the persistent /data volume.
# For local development, use ./data/curio.db instead.
DATABASE_PATH=/data/curio.db

# Optional Telegram destination. Configure both or neither.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Optional control-plane webhook. Configure an allowlisted numeric user ID when enabled.
TELEGRAM_WEBHOOK_SECRET=
TELEGRAM_WEBHOOK_URL=
TELEGRAM_ALLOWED_USER_IDS=
# Optional comma-separated numeric chat IDs. Empty means any chat for an allowed user.
TELEGRAM_ALLOWED_CHAT_IDS=

# Optional X source credentials for the read-only xbird adapter. Configure both or neither.
# Use a dedicated read account. These session cookies still carry full account authority.
X_AUTH_TOKEN=
X_CT0=

# Optional override; defaults to the migrations/ directory bundled with Curio.
# MIGRATIONS_PATH=/app/migrations

# Docker network already present on personal-vps.
CURIO_NETWORK=personal-infra_private
